const expect = require('chai').expect;
describe('some tests', () =>{
    it('should pass', () => {
        let result = true;
        expect(result).to.be.true;

      ;
    });

    it('should fail', () => {
        ;
      });
    
})